# OmniCore 能力拓展方向规划

> 编写时间：2026-03-28
> 基于当前架构（7 层 + 5 执行 Agent + LangGraph DAG 编排）的下一步拓展方向。

---

## 当前能力边界

| 维度 | 已具备 | 尚未覆盖 |
|------|--------|----------|
| 任务规划 | LLM 路由 + 自动分解 + 3 次 replan | 跨 Agent 协商、动态子图 |
| 执行能力 | Browser / Web / File / System / Terminal 五种 Agent | 外部工具生态（MCP）、多模态输入输出 |
| 记忆 | ChromaDB 向量检索 + 会话/任务/制品持久化 | 主动经验沉淀（Skill Library）、深度 RAG |
| 交互 | CLI + Streamlit Web UI | 语音、移动端、IM Bot |
| 自动化 | 定时任务 + 目录监控 + 后台 Worker | 事件驱动（网页变化监控、消息监听） |
| 成本控制 | 多模型支持、models.yaml 配置 | 基于复杂度的智能路由 |

---

## 拓展方向

### P0-1: MCP (Model Context Protocol) 工具生态接入

**价值**：投入产出比最高。一行配置接入社区数百个现成工具（数据库查询、Slack/飞书/邮件、日历、GitHub API 等），避免为每个外部服务单独写 Agent。

**当前基础**：
- `core/tool_registry.py` + `core/tool_protocol.py` 已有插件化框架
- `config/agents.yaml` 支持声明式工具注册
- Router 已具备向不同 tool 分发任务的能力

**实现路径**：
1. 在 `core/tool_adapters.py` 新增 `MCPToolAdapter`，通过 stdio/SSE 与 MCP Server 通信
2. MCP Server 工具列表在启动时自动发现，动态注入 Router prompt（复用 `{{AGENT_CAPABILITIES}}` 机制）
3. MCP 工具的输入/输出统一映射为 `TaskItem` / `ArtifactItem`
4. `config/mcp_servers.yaml` 声明式配置 MCP Server 列表

**预估工作量**：2-3 天核心实现 + 1 天联调

📄 **详细方案**：[2026-03-28-P0-1-MCP工具生态接入方案.md](2026-03-28-P0-1-MCP工具生态接入方案.md)

#### 实施进度（P0-1 MCP 工具生态接入）— 2026-03-28

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | MCP Server 声明式配置 | `config/mcp_servers.yaml`（新建） | ✅ 已完成 |
| 2 | MCP 客户端核心实现 | `core/mcp_client.py`（新建） | ✅ 已完成 |
| 3 | MCPToolAdapter 适配器 | `core/tool_adapters.py` 追加 `MCPToolAdapter` + `_extract_mcp_text` | ✅ 已完成 |
| 4 | 动态工具注册 | `core/tool_registry.py` 追加 `_register_mcp_tools()` | ✅ 已完成 |
| 5 | agents.yaml 声明 | `config/agents.yaml` 追加 `mcp_handler` 条目 | ✅ 已完成 |
| 6 | 策略引擎 MCP 规则 | `core/policy_engine.py` 追加 MCP 高危词拦截分支 | ✅ 已完成 |
| 7 | MCP 配置项 | `config/settings.py` 追加 `MCP_ENABLED` / `MCP_TOOL_CALL_TIMEOUT` | ✅ 已完成 |
| 8 | 单元测试 | `tests/test_mcp_unit.py`（新建，32 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`config/mcp_servers.yaml`、`core/mcp_client.py`、`tests/test_mcp_unit.py`
**修改文件**：`core/tool_adapters.py`、`core/tool_registry.py`、`config/agents.yaml`、`core/policy_engine.py`、`config/settings.py`

**架构说明**：
- `MCPClientManager` 单例管理所有 MCP Server 连接，启动时自动 discover 工具列表
- 工具以 `mcp.{server}.{tool}` 格式命名，注入 Router prompt（`{{AGENT_CAPABILITIES}}`）
- 支持三种传输：stdio（subprocess）、SSE（eventstream）、streamable HTTP
- 策略引擎对高危操作（delete/push/send_message/create_issue 等）强制人工确认

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `MCP_ENABLED` | `true` | MCP 功能总开关 |
| `MCP_TOOL_CALL_TIMEOUT` | `30` | 单次工具调用超时秒数 |

**如何接入新 MCP Server**：编辑 `config/mcp_servers.yaml`，添加 server 条目并设置 `enabled: true`，重启服务后工具自动注册。

**尚未实施**：
- SSE / HTTP 传输协议的端到端联调（当前仅 stdio 有完整测试）
- MCP Server 连接失败时的自动重连逻辑
- 工具调用结果的流式输出支持

---

### P0-2: 自主学习与经验复用（Skill Library）

**价值**：让系统越用越聪明，核心差异化能力。成功完成的任务流自动沉淀为可复用 Skill，下次遇到类似任务直接执行。

**当前基础**：
- ChromaDB 记忆系统已就绪
- `finalize` 阶段已有结果汇总逻辑
- `ArtifactStore` 可存储 Skill 产物

**实现路径**：
1. 定义 `Skill` 数据结构：名称、触发条件（语义描述）、参数模板、任务流模板
2. 在 `finalize` 阶段增加 Skill 提炼逻辑：LLM 判断当前任务是否值得沉淀 → 抽象为 Skill → 存入 ChromaDB 专用 collection
3. Router 阶段先检索 Skill Library：命中（相似度 > 阈值）→ 参数适配 → 直接执行；不命中 → 走完整规划
4. Skill 支持版本管理和失败自动降级（连续失败 N 次后标记为 deprecated）

**预估工作量**：3-4 天

📄 **详细方案**：[2026-03-28-P0-2-Skill-Library经验复用方案.md](2026-03-28-P0-2-Skill-Library经验复用方案.md)

#### 实施进度（P0-2 Skill Library 经验复用）— 2026-03-29

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | SkillDefinition 数据模型 + SkillStore 核心类 | `memory/skill_store.py`（新建） | ✅ 已完成 |
| 2 | Skill 提炼 LLM Prompt | `prompts/skill_extraction.txt`（新建） | ✅ 已完成 |
| 3 | Skill 参数提取 LLM Prompt | `prompts/skill_param_extraction.txt`（新建） | ✅ 已完成 |
| 4 | Skill Library 配置项 | `config/settings.py` 追加 `SKILL_LIBRARY_ENABLED` / `SKILL_MATCH_THRESHOLD` / `SKILL_MIN_STEPS_TO_EXTRACT` / `SKILL_AUTO_DEPRECATE_*` | ✅ 已完成 |
| 5 | State 扩展 | `core/state.py` 追加 `matched_skill_id` 字段 | ✅ 已完成 |
| 6 | Router 集成 Skill 匹配 | `core/router.py` `route()` 方法在 LLM 规划前检索 Skill Library | ✅ 已完成 |
| 7 | Finalize 集成提炼 + 反馈 | `core/graph.py` `finalize_node()` 末尾触发提炼和反馈更新 | ✅ 已完成 |
| 8 | CLI `/skills` 命令 | `main.py` 新增 `_handle_skills_command` + `/skills` 路由 | ✅ 已完成 |
| 9 | 单元测试 | `tests/test_skill_store_unit.py`（新建，48 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`memory/skill_store.py`、`prompts/skill_extraction.txt`、`prompts/skill_param_extraction.txt`、`tests/test_skill_store_unit.py`
**修改文件**：`config/settings.py`、`core/state.py`、`core/router.py`、`core/graph.py`、`main.py`

**架构说明**：
- `SkillStore` 使用独立 ChromaDB collection `omnicore_skills`，与通用记忆隔离
- Skill 以语义描述做向量检索，metadata 携带完整定义（task_template_json, parameters_json 等）
- 提炼流程：finalize 阶段 → 检查全部任务成功且 ≥ 2 步 → LLM 判断是否值得沉淀 → 语义去重 → 写入 ChromaDB
- 匹配流程：Router.route() 入口 → 向量检索 top-3 → 过滤 deprecated / 低成功率 → LLM 提取参数 → 实例化 task_queue → 跳过完整规划
- 反馈流程：finalize 阶段根据 `matched_skill_id` 更新使用统计，成功率 < 30% 且使用 ≥ 3 次自动降级

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `SKILL_LIBRARY_ENABLED` | `true` | Skill Library 功能总开关 |
| `SKILL_MATCH_THRESHOLD` | `0.3` | 向量检索距离阈值（越小越严格） |
| `SKILL_MIN_STEPS_TO_EXTRACT` | `2` | 提炼最低步骤数 |
| `SKILL_AUTO_DEPRECATE_THRESHOLD` | `0.3` | 自动降级成功率阈值 |
| `SKILL_AUTO_DEPRECATE_MIN_USES` | `3` | 自动降级最低使用次数 |

**CLI 命令**：
| 命令 | 说明 |
|------|------|
| `/skills` | 列出活跃 Skill |
| `/skills --all` | 列出所有 Skill（含 deprecated） |
| `/skills deprecate <id>` | 手动废弃 Skill |
| `/skills delete <id>` | 删除 Skill |

**尚未实施**：
- 端到端联调（执行任务 → 自动提炼 → 下次匹配 → 直接执行）
- Skill 版本升级机制（同名 Skill 新版本替换旧版本）
- Streamlit UI 中的 Skill 管理面板

---

### P1-1: 远程访问 — IM Bot 接入

**价值**：个人助手必须随时可达。手机上下发任务，后台执行，完成后推送通知。

**当前基础**：
- `submit_job` + `RuntimeStateStore` 已支持异步任务提交和状态查询
- 后台 Worker 已能独立消费队列
- 通知机制（`workflow_automation_store`）已有基础

**实现路径**：
1. 在 Layer 1（交互层）新增 `interactions/telegram_bot.py`（或微信/飞书 Bot）
2. Bot 接收消息 → 调用 `submit_job` → 返回 job_id
3. Worker 完成后通过 Bot API 推送结果
4. 支持 `/status`、`/cancel`、`/approve` 等交互命令
5. 高风险操作的审批通过 Bot 内联按钮完成

**预估工作量**：2-3 天（Telegram 最快，微信需额外适配）

📄 **详细方案**：[2026-03-28-P1-1-IM-Bot远程访问方案.md](2026-03-28-P1-1-IM-Bot远程访问方案.md)

---

### P1-2: 知识库 & RAG 深度集成

**价值**：已有 ChromaDB 基础设施，补全最后一公里。将浏览/下载/执行产出的信息自动索引，任务执行时自动检索相关知识。

**当前基础**：
- `memory/scoped_chroma_store.py` 已有向量存储和检索
- `memory/manager.py` 已有实体提取
- `ArtifactStore` 记录所有产物

**实现路径**：
1. Artifact 写入时自动触发文本提取 + 向量索引（PDF/HTML/文本文件）
2. 浏览器 Agent 完成后，将页面关键内容自动索引
3. Router 阶段增加 RAG 检索步骤：检索 top-K 相关知识 → 注入任务上下文
4. 提供 `/learn <url_or_file>` 命令让用户主动喂文档
5. 知识条目支持过期标记和手动管理

**预估工作量**：3-4 天

📄 **详细方案**：[2026-03-28-P1-2-知识库RAG深度集成方案.md](2026-03-28-P1-2-知识库RAG深度集成方案.md)

#### 实施进度（P1-2 知识库 RAG 深度集成）— 2026-03-29

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | KnowledgeStore 核心类 | `memory/knowledge_store.py`（新建） | ✅ 已完成 |
| 2 | Knowledge Base 配置项 | `config/settings.py` 追加 `KNOWLEDGE_BASE_ENABLED` / `KNOWLEDGE_RETRIEVAL_TOP_K` / `KNOWLEDGE_MAX_CONTEXT_CHARS` / `KNOWLEDGE_DISTANCE_THRESHOLD` / `KNOWLEDGE_MIN_CONTENT_LENGTH` | ✅ 已完成 |
| 3 | RAG 检索注入 Router | `core/router.py` `route()` 方法在 Skill 匹配后、LLM 规划前检索知识库，`analyze_intent()` 新增 `knowledge_context` 参数 | ✅ 已完成 |
| 4 | 自动索引集成 finalize | `core/graph.py` `finalize_node()` 末尾触发任务结果 + 浏览器产出索引 | ✅ 已完成 |
| 5 | CLI `/learn` 命令 | `main.py` 新增 `_handle_learn_command`，支持 URL 和本地文件 | ✅ 已完成 |
| 6 | CLI `/knowledge` 命令 | `main.py` 新增 `_handle_knowledge_command`，支持 search / stats / delete | ✅ 已完成 |
| 7 | 单元测试 | `tests/test_knowledge_store_unit.py`（新建，42 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`memory/knowledge_store.py`、`tests/test_knowledge_store_unit.py`
**修改文件**：`config/settings.py`、`core/router.py`、`core/graph.py`、`main.py`

**架构说明**：
- `KnowledgeStore` 使用独立 ChromaDB collection `omnicore_knowledge`，与通用记忆和 Skill Library 隔离
- 基于 `ChromaMemory` 的 `add_memory()` / `search_memory()` 接口，复用现有向量存储能力
- 文本分块：1000 字符窗口 + 200 字符重叠，确保语义连贯性
- 自动索引：finalize 阶段对成功任务结果 + 浏览器抓取网页自动索引（非阻塞）
- RAG 注入：Router 在 Skill 匹配失败后、LLM 规划前检索 top-3 知识 → 格式化为 `## 相关知识` 段落注入 user_message
- 距离过滤：默认 > 0.5 的低相关结果被过滤，总上下文限制 4000 字符

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `KNOWLEDGE_BASE_ENABLED` | `true` | 知识库功能总开关 |
| `KNOWLEDGE_RETRIEVAL_TOP_K` | `5` | 每个类型检索的条目数 |
| `KNOWLEDGE_MAX_CONTEXT_CHARS` | `4000` | 注入 LLM 上下文的最大字符数 |
| `KNOWLEDGE_DISTANCE_THRESHOLD` | `0.5` | 向量距离阈值（越小越严格） |
| `KNOWLEDGE_MIN_CONTENT_LENGTH` | `50` | 索引最低内容长度 |

**CLI 命令**：
| 命令 | 说明 |
|------|------|
| `/learn <url>` | 抓取网页并索引到知识库 |
| `/learn <file_path>` | 解析本地文件并索引到知识库 |
| `/knowledge stats` | 查看知识库统计 |
| `/knowledge search <query>` | 语义检索知识库 |
| `/knowledge delete <source>` | 按来源删除知识条目 |

**尚未实施**：
- 端到端联调（索引 → 检索 → 注入 → 验证效果）
- 知识条目过期清理策略（90 天 + 低命中自动归档）
- Streamlit UI 中的知识库管理面板
- HTML 格式文件解析支持（需 beautifulsoup4）

---

### P2-1: 多模态输入/输出

**价值**：提升交互体验。用户贴图问"帮我找到这个产品"、上传 PDF 提取信息后执行后续任务、语音下发指令。

**当前基础**：
- Vision Model 已接入（`gpt-4o`）
- Browser Agent 已有截图分析能力
- LiteLLM 支持多模态 API

**实现路径**：
1. **图片/文档输入**：CLI 和 Streamlit 支持文件上传 → 自动调用 Vision Model 提取意图 → 注入 Router
2. **语音输入**：接入 Whisper API → 转文本 → 走正常流程
3. **语音输出**：结果文本 → TTS API → 音频播放/推送
4. **文档理解**：PDF/Word/Excel → 文本提取 → LLM 摘要 → 作为任务上下文

**预估工作量**：3-5 天（图片/文档优先，语音可后置）

📄 **详细方案**：[2026-03-28-P2-1-多模态输入输出方案.md](2026-03-28-P2-1-多模态输入输出方案.md)

#### 实施进度（P2-1 多模态输入/输出）— 2026-03-28

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | 文档文本提取器 | `utils/document_parser.py`（新建） | ✅ 已完成 |
| 2 | 多模态输入处理器 | `utils/multimodal_input.py`（新建） | ✅ 已完成 |
| 3 | CLI `/attach` 命令 | `main.py` 新增 `_handle_attach_command` + `/attach` 路由 | ✅ 已完成 |
| 4 | Streamlit 文件上传 | `ui/app.py` 新增 `st.file_uploader` + 多模态预处理 | ✅ 已完成 |
| 5 | 多模态配置项 | `config/settings.py` 新增 `MULTIMODAL_*` / `VOICE_OUTPUT_*` 共 6 项 | ✅ 已完成 |
| 6 | 可选语音输出（TTS） | `core/runtime.py` `_finalize_runtime_result` 末尾追加 TTS 钩子 | ✅ 已完成 |
| 7 | 单元测试 | `tests/test_multimodal_unit.py`（新建，21 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`utils/document_parser.py`、`utils/multimodal_input.py`、`tests/test_multimodal_unit.py`
**修改文件**：`main.py`、`ui/app.py`、`config/settings.py`、`core/runtime.py`

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `MULTIMODAL_IMAGE_ENABLED` | `true` | 图片输入开关 |
| `MULTIMODAL_AUDIO_ENABLED` | `false` | 语音输入开关（需 OpenAI Whisper API） |
| `MULTIMODAL_DOCUMENT_ENABLED` | `true` | 文档输入开关 |
| `VOICE_OUTPUT_ENABLED` | `false` | TTS 语音输出开关（需 OpenAI TTS API） |
| `VOICE_OUTPUT_MODEL` | `tts-1` | TTS 模型 |
| `VOICE_OUTPUT_VOICE` | `alloy` | TTS 声音类型 |

**可选依赖**（缺失时对应格式静默跳过）：
- `PyPDF2` — PDF 文本提取
- `python-docx` — DOCX 文本提取

**尚未实施**：
- 端到端联调（图片/文档/语音全链路）
- Streamlit 语音播放组件（`st.audio` 展示 TTS 结果）

---

### P2-2: 成本感知与智能路由

**价值**：长期运行显著省钱。简单任务用便宜模型，复杂推理用强模型。

**当前基础**：
- `config/models.yaml` 已有多模型 + cost_preference 配置
- `core/llm.py` 已有模型切换能力
- Router 已有意图分类

**实现路径**：
1. Router 增加 complexity scoring（基于任务描述长度、关键词、历史相似任务的 token 消耗）
2. `core/llm.py` 基于 score 动态路由：低复杂度 → DeepSeek/Haiku，高复杂度 → Claude/GPT-4o
3. 记录每次调用的模型 + token + 成本到 `RuntimeMetricsStore`
4. 提供 `/cost` 命令查看历史成本统计
5. 支持用户设置月度预算上限

**预估工作量**：2-3 天

📄 **详细方案**：[2026-03-28-P2-2-成本感知智能路由方案.md](2026-03-28-P2-2-成本感知智能路由方案.md)

#### 实施进度（P2-2 成本感知智能路由）— 2026-03-31

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | 模型定价表 | `config/model_pricing.yaml`（新建） | ✅ 已完成 |
| 2 | 复杂度评分器 | `core/complexity_scorer.py`（新建） | ✅ 已完成 |
| 3 | 成本计算器 + 月度预算守卫 | `utils/cost_tracker.py`（新建） | ✅ 已完成 |
| 4 | 配置项 | `config/settings.py` 追加 `COST_TRACKING_ENABLED` / `MONTHLY_BUDGET_USD` / `COMPLEXITY_AWARE_ROUTING` | ✅ 已完成 |
| 5 | LLMClient 复杂度感知路由 | `core/llm.py` `__init__()` 新增 `complexity` 参数 | ✅ 已完成 |
| 6 | LLMClient 成本记录 | `core/llm.py` `_build_llm_response()` 追加成本追踪 | ✅ 已完成 |
| 7 | Router 集成复杂度打分 | `core/router.py` `analyze_intent()` 末尾追加 | ✅ 已完成 |
| 8 | CLI `/cost` 命令 | `main.py` 新增 `_handle_cost_command` + `/cost` 路由 | ✅ 已完成 |
| 9 | 单元测试 | `tests/test_complexity_scorer_unit.py`（新建，45 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`config/model_pricing.yaml`、`core/complexity_scorer.py`、`utils/cost_tracker.py`、`tests/test_complexity_scorer_unit.py`
**修改文件**：`config/settings.py`、`core/llm.py`、`core/router.py`、`main.py`

**架构说明**：
- `score_task_complexity()` 按 4 个维度打分：步骤数(25%)、工具类型(35%)、依赖链深度(15%)、输入关键词(25%)
- 复杂度分段：< 0.35 → `low`（DeepSeek/Gemini Flash），0.35~0.65 → `medium`，≥ 0.65 → `high`（Claude/GPT-4o）
- `LLMClient(complexity=...)` 新参数：capability 模式下自动将复杂度映射为 `prefer_cost` 传入 `ModelRegistry`
- `_build_llm_response()` 每次调用后写 `data/monthly_cost.jsonl`，异常静默（不影响主流程）
- Router `analyze_intent()` 返回值新增 `task_complexity` / `recommended_cost_tier` 字段，下游可按需消费
- `COMPLEXITY_AWARE_ROUTING=false` 可完全关闭复杂度路由，退回原有行为

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `COST_TRACKING_ENABLED` | `true` | 成本追踪总开关 |
| `MONTHLY_BUDGET_USD` | `0` | 月度预算上限（0 表示不限制） |
| `COMPLEXITY_AWARE_ROUTING` | `true` | 复杂度感知路由总开关 |

**CLI 命令**：
| 命令 | 说明 |
|------|------|
| `/cost` | 查看本月 LLM 成本统计及按模型分布 |

**尚未实施**：
- 端到端联调验证（实际任务执行 → 成本记录 → `/cost` 显示全链路）
- `model_pricing.yaml` 价格表定期自动更新机制
- Streamlit UI 中的成本面板

---

### P3-1: 实时信息流 & 事件驱动

**价值**：从被动响应升级为主动监控。网页价格变动、新文章发布、邮件到达时自动触发任务。

**当前基础**：
- `workflow_automation_store.py` 已有目录监控和定时任务
- 后台 Worker 已能消费事件队列
- 通知基础设施已就绪

**实现路径**：
1. 抽象通用 `EventSource` 接口（目录监控、网页变化、邮件、Webhook 皆为实现）
2. `WebChangeMonitor`：定期 Browser/Web Agent 访问指定 URL → diff 内容 → 变化超阈值触发任务
3. `EmailMonitor`：IMAP 轮询 → 新邮件触发任务
4. `WebhookReceiver`：HTTP endpoint 接收外部事件
5. 事件 → 自动创建 Job → Worker 消费 → 结果通知

**预估工作量**：3-5 天（网页监控优先）

📄 **详细方案**：[2026-03-28-P3-1-事件驱动信息流方案.md](2026-03-28-P3-1-事件驱动信息流方案.md)

#### 实施进度（P3-1 事件驱动信息流）— 2026-03-31

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | EventSource 协议 + WatchEvent 数据模型 | `utils/event_sources/base.py`（新建） | ✅ 已完成 |
| 2 | WebPageWatchSource 网页变化监控 | `utils/event_sources/web_page_watch.py`（新建） | ✅ 已完成 |
| 3 | WebhookSource HTTP 事件接收 | `utils/event_sources/webhook_source.py`（新建） | ✅ 已完成 |
| 4 | EmailWatchSource IMAP 邮件监控 | `utils/event_sources/email_watch.py`（新建） | ✅ 已完成 |
| 5 | EventDispatcher 统一事件分发器 | `utils/event_dispatcher.py`（新建） | ✅ 已完成 |
| 6 | 集成到 Worker 循环 | `core/runtime.py` `run_next_queued_task()` 追加 dispatcher 调用 | ✅ 已完成 |
| 7 | 事件驱动配置项 | `config/settings.py` 追加 `EVENT_DRIVEN_ENABLED` / `WEB_WATCH_*` / `WEBHOOK_*` | ✅ 已完成 |
| 8 | CLI `/watch` 命令 | `main.py` 新增 `_handle_watch_command`，支持 url/list/pause/resume/stop | ✅ 已完成 |
| 9 | 单元测试 | `tests/test_event_sources_unit.py`（新建，43 个用例，全部通过） | ✅ 已完成 |

**新建文件**：`utils/event_sources/__init__.py`、`utils/event_sources/base.py`、`utils/event_sources/web_page_watch.py`、`utils/event_sources/webhook_source.py`、`utils/event_sources/email_watch.py`、`utils/event_dispatcher.py`、`tests/test_event_sources_unit.py`
**修改文件**：`config/settings.py`、`core/runtime.py`、`main.py`

**架构说明**：
- `EventSource` Protocol 定义统一接口：`poll_events()`、`create_watch()`、`pause_watch()`、`resume_watch()`、`delete_watch()`、`list_watches()`
- `WatchEvent` 数据类统一所有事件源的输出格式，携带 `source_type`、`trigger_data`、`user_input_template` 等
- `EventDispatcher` 单例管理所有事件源，在 Worker 轮询中统一调用 `dispatch_pending_events()`
- 网页监控：requests 轻量抓取 → BeautifulSoup 提取文本 → MD5 hash + 词级变化率判断 → 超阈值触发
- 邮件监控：IMAP 标准库连接 → UID 增量拉取 → 主题/发件人过滤 → 匹配时触发
- Webhook：内嵌 HTTPServer 守护线程 → POST /webhook/{watch_id} → 队列缓冲 → poll 消费
- 所有事件源状态持久化到 `data/` 下的 JSONL/JSON 文件

**配置项说明**：
| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `EVENT_DRIVEN_ENABLED` | `true` | 事件驱动功能总开关 |
| `WEB_WATCH_MIN_INTERVAL` | `300` | 网页检查最小间隔（秒） |
| `WEB_WATCH_DEFAULT_INTERVAL` | `3600` | 网页检查默认间隔（秒） |
| `WEB_WATCH_DEFAULT_THRESHOLD` | `0.1` | 网页变化触发阈值（0~1） |
| `WEBHOOK_ENABLED` | `false` | Webhook HTTP 服务器开关 |
| `WEBHOOK_PORT` | `9988` | Webhook 监听端口 |

**CLI 命令**：
| 命令 | 说明 |
|------|------|
| `/watch url <url> [备注]` | 创建网页变化监控 |
| `/watch list` | 列出所有监控项 |
| `/watch pause <id>` | 暂停监控 |
| `/watch resume <id>` | 恢复监控 |
| `/watch stop <id>` | 删除监控 |

**尚未实施**：
- 端到端联调（网页变化 → 自动触发任务 → 结果通知全链路）
- 邮件监控端到端联调（需配置真实 IMAP 账号）
- Webhook 端到端联调（启动 HTTP 服务器 → 外部 POST → 任务触发）
- 现有 DirectoryWatchSource / ScheduleSource 的 EventSource 接口适配（当前仍走独立的 `_release_*()` 函数）

---

### P3-2: 多 Agent 协作（Agent-to-Agent） ✅ 已实施（2026-03-31）

**价值**：支持复杂的跨 Agent 联动场景。

📄 **详细方案**：[2026-03-28-P3-2-多Agent协作方案.md](2026-03-28-P3-2-多Agent协作方案.md)

**已实施内容**：

| 步骤 | 任务 | 涉及文件 | 状态 |
|------|------|---------|------|
| 1 | `TaskOutputType` 枚举 + `OmniCoreState` 扩展（`task_outputs` / `dynamic_task_additions` / `agent_messages`） | `core/constants.py`, `core/state.py` | ✅ |
| 2 | `_extract_typed_output()` 任务输出类型化提取 | `core/task_executor.py` | ✅ |
| 3 | `_resolve_task_params()` 支持 `$task_id.field` 跨任务数据引用 | `core/task_executor.py` | ✅ |
| 4 | `_evaluate_condition()` + `is_task_ready()` 条件任务检查（`ends_with`/`starts_with`/`contains`/`==`/`!=`/`exists`） | `core/task_executor.py` | ✅ |
| 5 | `dynamic_replan_node` 动态规划节点 + 图连接（含循环依赖检测、插入上限） | `core/graph.py` | ✅ |
| 6 | Agent-to-Agent 消息类型（`MSG_AGENT_REQUEST`/`MSG_AGENT_RESPONSE`/`MSG_AGENT_STATUS`）+ `AgentRequest`/`AgentResponse` 数据类 | `core/message_bus.py` | ✅ |
| 7 | Router prompt 支持条件任务声明 + `$ref` 参数引用说明 | `prompts/router_system.txt` | ✅ |
| 8 | 单元测试 42 项全部通过 | `tests/test_multi_agent_unit.py` | ✅ |

**尚未实施**：
- 端到端联调（Browser 下载 → Terminal 处理 → File 整理全链路）
- Planner Agent 主动动态调整（当前仅支持被动的 `dynamic_task_additions` 插入）
- Agent 间协商协议完整实现（三步握手：请求-响应-确认）

---

## 优先级总览

| 优先级 | 方向 | 核心理由 |
|--------|------|----------|
| **P0** | MCP 工具生态接入 | ✅ 已实施（2026-03-28） |
| **P0** | Skill Library 经验复用 | ✅ 已实施（2026-03-29） |
| **P1** | IM Bot 远程访问 | 个人助手必须随时可达 |
| **P1** | 知识库 RAG 深度集成 | ✅ 已实施（2026-03-29） |
| **P2** | 多模态输入/输出 | ✅ 已实施（2026-03-28） |
| **P2** | 成本感知智能路由 | ✅ 已实施（2026-03-31） |
| **P3** | 事件驱动信息流 | ✅ 已实施（2026-03-31） |
| **P3** | 多 Agent 协作 | ✅ 已实施（2026-03-31） |
